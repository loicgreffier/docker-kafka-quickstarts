# Base

This Docker Compose environment includes:

- KRaft broker
- Control Center

## Prerequisites

- Docker

## Running the Environment

To start the environment, run:

```bash
docker-compose up -d
```