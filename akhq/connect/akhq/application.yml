akhq:
  connections:
    docker-kafka-quickstarts-cluster:
      connect:
        - name: 'local'
          url: 'http://connect:8083'
      properties:
        bootstrap.servers: 'broker:29092'
      schema-registry:
        url: 'http://schema-registry:8081'